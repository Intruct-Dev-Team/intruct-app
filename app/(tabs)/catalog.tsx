import { PageHeader } from "@/components/page-header";
import { ScreenContainer } from "@/components/screen-container";

export default function CatalogScreen() {
  return (
    <ScreenContainer>
      <PageHeader title="Catalog" subtitle="Browse all available courses" />
    </ScreenContainer>
  );
}
